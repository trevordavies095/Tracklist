# Tracklist Configuration
# Copy this file to .env and modify the values as needed

# =================================
# Database Configuration
# =================================

# Option 1: Specify just the database file path (recommended for most users)
# This will automatically create the directory structure if it doesn't exist
# Examples:
#   TRACKLIST_DB_PATH=/home/user/music/tracklist.db
#   TRACKLIST_DB_PATH=/app/data/my-music-db.db
#   TRACKLIST_DB_PATH=./data/tracklist.db
TRACKLIST_DB_PATH=./data/tracklist.db

# Option 2: Full database URL (advanced users)
# If TRACKLIST_DB_PATH is set, this will be ignored
# Examples:
#   DATABASE_URL=sqlite:///./data/tracklist.db
#   DATABASE_URL=postgresql://user:pass@localhost/tracklist
#   DATABASE_URL=mysql://user:pass@localhost/tracklist
# DATABASE_URL=sqlite:///./data/tracklist.db

# =================================
# Application Configuration
# =================================

# Environment mode
ENVIRONMENT=production

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Secret key for session management (generate a secure random string)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here

# =================================
# MusicBrainz API Configuration
# =================================

# User agent for MusicBrainz API requests
# Should include your app name and contact info
MUSICBRAINZ_USER_AGENT=Tracklist/1.0 (your-email@example.com)

# Rate limiting for MusicBrainz API (requests per second)
MUSICBRAINZ_RATE_LIMIT=1.0

# =================================
# Server Configuration
# =================================

# Host to bind to (0.0.0.0 for Docker, localhost for local dev)
HOST=0.0.0.0

# Port to run on
PORT=8000

# Number of worker processes (for production)
WORKERS=1

# =================================
# Docker/Container Configuration
# =================================

# Timezone for container
TZ=UTC

# User ID for file permissions (useful for Docker)
# PUID=1000
# PGID=1000